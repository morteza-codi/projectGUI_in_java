name: Java CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        
    - name: Build with Java
      run: |
        mkdir -p out/production/Paint
        javac -d out/production/Paint src/*.java
        
    - name: List compiled files
      run: find out -name "*.class" | sort
      
    # If you add tests in the future, uncomment this
    # - name: Run tests
    #   run: java -cp out/production/Paint org.junit.runner.JUnitCore TestSuite
